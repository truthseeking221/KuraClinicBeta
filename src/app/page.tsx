import { OnboardingFlowPage } from './_demo/onboarding-flow-page';

/** Canonical entry: Door → onboarding → workspace gate → clinic app. */
export default function Entry() {
  return <OnboardingFlowPage />;
}
